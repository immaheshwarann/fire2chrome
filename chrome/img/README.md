# img/ — Extension Icons

This directory must contain the SVG icon files used by the extension toolbar button
and the browser's extension management page.  Extract them from the `.xpi` archive:

```
img/
├── clippings16.svg
├── clippings32.svg
├── icon32.svg
└── icon.svg
```
